# robots.txt for rctravel.pages.dev
# Travel Photography Portfolio by Romain Charretteur

# Allow all search engines to crawl the entire site
User-agent: *
Allow: /

# Sitemap location
Sitemap: https://rctravel.pages.dev/sitemap.xml

# Crawl-delay (optional, helps prevent server overload)
# Most modern bots ignore this, but it doesn't hurt
Crawl-delay: 1

# Disallow crawling of admin/system files (if any exist in the future)
# Disallow: /admin/
# Disallow: /.git/
